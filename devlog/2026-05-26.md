# 2026-05-26

## Completed

- Added a traditional macOS `Resources/AppIcon.icns` file generated from the existing app icon PNG assets.
- Added `CFBundleIconFile = AppIcon.icns` to `Resources/Info.plist`.
- Added `Resources/AppIcon.icns` to `project.yml` resources so XcodeGen-generated projects copy it into the app bundle.
- Confirmed from exported app bundle screenshot that `Contents/Resources` is missing, so Xcode is not copying bundle resources into the exported app.
- Confirmed the repository does not contain `PasteEcho.xcodeproj` because it is ignored as an XcodeGen-generated file; Xcode must be opened from a regenerated project.
- Added `fileGroups: Resources` to `project.yml` so the `Resources` folder appears in Xcode's project navigator after regeneration.

## Todo

- Regenerate `PasteEcho.xcodeproj` with XcodeGen, then clean build and archive again.
- After export, verify `PasteEcho.app/Contents/Resources/AppIcon.icns` exists.
- If Finder still shows a generic icon, refresh Finder/LaunchServices icon cache on the Mac.
- If not using the regenerated XcodeGen project, manually add `Resources/AppIcon.icns` and `Resources/Assets.xcassets` to the app target's Copy Bundle Resources phase in Xcode.
