# 2026-05-25

## Completed

- Fixed exported macOS app icon configuration.
- Added explicit `CFBundleIconName = AppIcon` to `Resources/Info.plist` so the asset catalog app icon is discoverable after archive/export.

## Todo

- Rebuild or archive the app in Xcode and verify the exported `.app` shows the PasteEcho icon in Finder.
- If Finder still shows the old blank icon, clear LaunchServices/Finder icon cache or rename the exported app for a fresh cache entry.
