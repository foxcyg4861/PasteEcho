import SwiftUI

extension Color {
    static let pasteEchoBlue = Color(red: 0.30, green: 0.60, blue: 0.95)
    static let pasteEchoPinnedTint = pasteEchoBlue.opacity(0.08)
    static let pasteEchoDestructive = Color.red.opacity(0.7)
}
